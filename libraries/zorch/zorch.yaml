id: zorch
name: zorch
type: core
tagline: >-
  JAX-native building blocks for Modern SNARKs (IOP + PCS).
  Proving-scheme- and zkVM-agnostic, fusion-first.
repo: https://github.com/fractalyze/zorch
# PyPI "zorch" is squatted by an unrelated package; the core ships as pyzorch.
pypi: pyzorch
# TODO(team): declare what the core ships (pcs/hash vocab). See design.md §8.
# The Run button's snippet lives in snippets/zorch.py (loaded by name).
